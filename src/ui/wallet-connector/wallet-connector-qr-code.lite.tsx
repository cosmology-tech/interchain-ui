import {
  useStore,
  useDefaultProps,
  useRef,
  onUpdate,
  onMount,
  useMetadata,
} from "@builder.io/mitosis";
import Box from "../box";
import type { WalletConnectorQRCodeProps } from "./wallet-connector.types";

export default function WalletConnectorQRCode(
  props: WalletConnectorQRCodeProps,
) {
  return null;
}
