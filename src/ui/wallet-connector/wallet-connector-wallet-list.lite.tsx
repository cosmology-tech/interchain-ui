import {
  useStore,
  useDefaultProps,
  useRef,
  onUpdate,
  onMount,
  useMetadata,
} from "@builder.io/mitosis";
import Box from "../box";
import type { WalletConnectorWalletListProps } from "./wallet-connector.types";

export default function WalletConnectorWalletList(
  props: WalletConnectorWalletListProps,
) {
  return null;
}
