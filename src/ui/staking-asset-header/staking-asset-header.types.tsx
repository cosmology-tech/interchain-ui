export interface StakingAssetHeaderProps {
  imgSrc: string;
  symbol: string;
  totalAmount: number;
  totalPrice: number;
  available: number;
  // ====
  totalLabel?: string;
  availableLabel?: string;
}
