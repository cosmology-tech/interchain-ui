import { generateButtonVars } from "../../ui/button/button.vars";
import { generateTabsVars } from "../../ui/tabs/tabs.vars";

export const slotVars = {
  ...generateButtonVars(),
  ...generateTabsVars(),
};
